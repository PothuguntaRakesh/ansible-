 - name: conditions
   hosts: web 
   become: yes
   tasks: 
    - name: check user exists 
      ansible.builtin.command: id Experience


    - name : say "hello
      ansible.builtin.debug:
       msg: "hello"   